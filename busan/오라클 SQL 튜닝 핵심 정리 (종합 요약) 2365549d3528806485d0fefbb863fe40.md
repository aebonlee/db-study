# 오라클 SQL 튜닝 핵심 정리 (종합 요약)

## 1. 🔍 SQL 튜닝의 목적과 기본 원칙

- **목적:** SQL 실행 속도 개선이 아니라 **불필요한 자원 낭비 제거**
- **대상:** CPU 사용 과다, I/O 발생량 과다, 공유 실패, 병목 지점
- **원칙:**
    - 실행계획 해석 → 병목 파악
    - 조건절 구조 개선 → 인덱스 활용 유도
    - 튜닝 전후 비교는 수치로 증명 (COST, GETS, READS 등)

---

## 2. 📊 실행계획 해석의 기본

| 항목 | 의미 | 주의점 |
| --- | --- | --- |
| **Id** | 실행 순서 아님, 계층 구조 | 하위 Operation부터 실행 |
| **Operation** | 처리 방식 | FULL SCAN, INDEX SCAN 등 |
| **Rows** | 예상 리턴 행 수 | 통계 기반 예측 |
| **Cost** | 옵티마이저 판단 기준 | 상대적 비교 수치 |
| **Access/Filter** | 인덱스 조건 vs 후처리 조건 | Access만 인덱스 활용 |

---

## 3. 🔧 조건절 튜닝 전략

| 문제 유형 | 개선 전략 |
| --- | --- |
| `TO_CHAR(HIREDATE)` | 함수 제거 → `BETWEEN` 날짜 범위 사용 |
| `LIKE '%전자%'` | 구조 변경 or Full Text Index |
| `OR 조건` | `UNION ALL`로 분해 |
| `NOT IN`, `!=` | `NOT EXISTS`, `ANTI JOIN` |
| `컬럼 + 1 = 100` | 산술 연산 제거, 등가 조건 유지 |

✅ **원칙:** 인덱스가 걸릴 수 있도록 조건절 구조를 단순하고 명확하게 구성

---

## 4. 📁 인덱스 설계 전략

- **선행 컬럼 원칙:** 다중 컬럼 인덱스에서 첫 번째 컬럼이 WHERE절에 있어야 함
- **Function-Based Index:** 함수가 불가피할 경우 생성
- **불필요 인덱스 제거:** 사용률 낮고 DML 부하 크면 DROP 고려
- **유지 기준:**
    - BLEVEL ≥ 3, LEAF_BLOCKS ↑, CLUSTERING_FACTOR ≒ ROWS → REBUILD

---

## 5. 🧠 힌트 사용 전략

| 힌트 | 의미 |
| --- | --- |
| `INDEX(TABLE)` | 인덱스 사용 유도 |
| `FULL(TABLE)` | Full Scan 유도 |
| `LEADING(A B)` | 조인 순서 고정 |
| `USE_NL(B)` | Nested Loop Join 유도 |
| `USE_HASH(B)` | Hash Join 유도 |
| `MERGE(V)` / `NO_MERGE(V)` | 뷰 병합 여부 결정 |
| `PARALLEL(TABLE n)` | 병렬 실행 유도 |

⚠ 힌트는 옵티마이저가 잘못 판단했을 때 **예외적으로 제한적으로** 사용해야 함

---

## 6. 🔁 조인 튜닝 전략

| 조인 방식 | 특징 | 적용 상황 |
| --- | --- | --- |
| Nested Loop | 소량 반복 조인 | 인덱스 있고 루프 작을 때 |
| Hash Join | 대용량 조인 | 인덱스 없거나 집계 포함 시 |
| Merge Join | 정렬된 테이블 간 | Index Sort 가능 시 |
- OUTER JOIN은 옵티마이저 조인 순서 변경 불가 → 전략적 힌트 필요
- 조인 순서 힌트: `LEADING`, `ORDERED`, `USE_*`

---

## 7. 🧮 통계와 옵티마이저 조정

- 옵티마이저는 통계 기반으로 실행계획 결정
- **히스토그램 필요 시점:** 특정 값 편중이 매우 큰 경우
- 수집 명령:
    
    ```sql
    EXEC DBMS_STATS.GATHER_TABLE_STATS(…);
    ```
    

| 히스토그램 종류 | 설명 |
| --- | --- |
| FREQUENCY | 데이터 값 별 비율 반영 |
| HEIGHT BALANCED | 구간 나눔 중심 |
| TOP-FREQUENCY | 상위 빈도값 중심 (12c 이후) |

---

## 8. 🛠 병렬 처리(PQ) 전략

- 병렬 구조: Query Coordinator + PX 서버들
- 힌트:
    
    ```sql
    SELECT /*+ PARALLEL(TABLE 4) */ …
    ```
    
- 실행계획: `PX SEND`, `PX BLOCK`, `PX COORDINATOR`
- 진단: `V$PX_SESSION`, `V$PQ_TQSTAT`

⚠ 병렬 처리 시 자원 과다 사용, Wait 발생 가능 → 테스트 후 적용

---

## 9. 🔒 Lock, Wait, 트랜잭션 튜닝

- 성능 저하가 SQL 때문이 아니라 **락 대기 때문**일 수 있음
- 진단:
    
    ```sql
    SELECT * FROM V$SESSION WHERE WAIT_CLASS != 'Idle';
    SELECT * FROM V$LOCK WHERE BLOCK = 1;
    ```
    
- 개선 전략:
    - COMMIT 시점 조정
    - 트랜잭션 분할
    - `SKIP LOCKED`, `NOWAIT` 활용

---

## 10. 📄 튜닝 보고서/발표 구조

| 항목 | 내용 |
| --- | --- |
| 대상 SQL 설명 | 기능, 실행빈도 등 |
| 병목 원인 분석 | 실행계획, COST, GETS 등 |
| 개선 전략 | 조건절, 인덱스, 힌트, 통계 |
| 개선 결과 | 전후 수치 비교 (표 작성 필수) |
| 결론 | 적용성, 유의점, 향후 과제 등 |

🎤 발표는 **5분 내외 + 수치 기반 설득력**이 핵심

---

## 🧩 최종 정리 문장

> “SQL 튜닝은 속도를 높이는 기술이 아니라,
구조를 읽고 자원 낭비를 제거하는 해석의 예술이다.”
>